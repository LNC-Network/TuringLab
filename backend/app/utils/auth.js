function AuthUser(username,password){
    // check username and password from database
    return true;
}
function HashPassword(password){
    // get password and return the hash and salt
    let hash;
    return hash;
}
function checkUserNamePasswordAvailability(username,password){
    // lookup
    return true;
}